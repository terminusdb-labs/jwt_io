image: swipl:latest

before_script:
  - apt-get update -qq
  - apt-get install -y -qq software-properties-common
  - add-apt-repository ppa:ben-collins/libjwt -y
  - apt-get update -qq && apt-get install -y -qq gcc make libjwt

test:
  script:
    - make check
